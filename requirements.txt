# Core dependencies
openai>=1.0.0
requests>=2.28.0
# Note: Limiting Pydantic version for FastAPI compatibility
# Using Pydantic v1 series because FastAPI 0.100.0 doesn't support Pydantic v2
pydantic>=1.10.0,<2.0.0
python-dotenv>=1.0.0
httpx>=0.25.0
tenacity>=8.2.0

# AI and LangChain
langchain-core>=0.1.0

# API and Web servers
fastapi>=0.95.0,<0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
flask>=3.0.0
flask-cors>=4.0.0
flask-session>=0.5.0
flask-login>=0.6.2

# Authentication
authlib>=1.2.0
# 注册/登录都要用（auth/routes.py:195 和 :335 的函数内 import）。
# 之前漏在这里，导致 /api/auth/register 和 /login 一调用就 500。
bcrypt>=4.0.0

# Data and storage
SQLAlchemy>=2.0.0
tiktoken>=0.5.0
pandas>=2.0.0
surrealdb>=1.0.4

# Testing and development tools
pytest>=7.0.0
coverage>=7.0.0
